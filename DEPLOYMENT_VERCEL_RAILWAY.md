# Bentana Insights - Vercel & Railway 部署指南

本指南專門針對使用 Vercel（前端）和 Railway（後端）的部署流程。

## 📋 部署架構

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   Vercel    │─────▶│   Railway    │─────▶│   MySQL     │
│  (Frontend) │      │  (Backend)   │      │  (Database) │
└─────────────┘      └──────────────┘      └─────────────┘
```

## 🚀 快速開始

### 前置要求

- GitHub 帳號
- Vercel 帳號（免費）
- Railway 帳號（免費 $5/月額度）
- OpenRouter API Key

---

## 🚂 步驟 1：部署後端到 Railway

### 1.1 創建 Railway 項目

1. 訪問 https://railway.app
2. 點擊「New Project」
3. 選擇「Deploy from GitHub repo」
4. 選擇 `polymarket-insights` repository
5. Railway 會自動檢測並開始構建

### 1.2 添加 MySQL 數據庫

1. 在 Railway 項目中，點擊「+ New」
2. 選擇「Database」→「Add MySQL」
3. Railway 會自動創建並生成 `DATABASE_URL`

### 1.3 配置環境變量

在 Railway 項目 → Variables 中添加：

```
DATABASE_URL=（Railway 自動提供）
JWT_SECRET=（使用 openssl rand -base64 32 生成）
OPENROUTER_API_KEY=（從 https://openrouter.ai 獲取）
```

### 1.4 運行數據庫遷移

在 Railway Terminal 中執行：
```bash
pnpm db:push
```

### 1.5 獲取後端 URL

部署成功後，保存 Railway 生成的 URL：
```
https://polymarket-insights-production.up.railway.app
```

---

## 🔷 步驟 2：部署前端到 Vercel

### 2.1 導入 GitHub Repository

1. 訪問 https://vercel.com
2. 點擊「Add New...」→「Project」
3. 選擇 `polymarket-insights` repository
4. 點擊「Import」

### 2.2 配置構建設置

**Framework Preset:** Vite

**Build Command:**
```
cd client && pnpm install && pnpm build
```

**Output Directory:**
```
client/dist
```

### 2.3 配置環境變量

在 Vercel → Settings → Environment Variables 中添加：

```
VITE_APP_TITLE=Bentana Insights
VITE_APP_LOGO=/logo.svg
VITE_API_URL=https://your-railway-backend.railway.app
```

**注意**：將 `VITE_API_URL` 替換為步驟 1.5 的 Railway URL。

### 2.4 更新 vercel.json

確保 API 代理配置正確：

```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://your-railway-backend.railway.app/api/:path*"
    }
  ]
}
```

提交並推送到 GitHub 觸發重新部署。

---

## ✅ 步驟 3：測試驗證

### 3.1 檢查前端
- ✅ 頁面正常加載
- ✅ 樣式正確顯示
- ✅ 沒有 Console 錯誤

### 3.2 檢查 API
- ✅ API 請求成功（狀態碼 200）
- ✅ 數據正確返回
- ✅ 沒有 CORS 錯誤

### 3.3 檢查數據庫
- ✅ 表已創建
- ✅ 數據正常寫入

---

## 🔍 故障排除

### 問題：前端無法連接後端

**解決方案：**
1. 檢查 `VITE_API_URL` 環境變量
2. 檢查 `vercel.json` API 代理配置
3. 檢查 Railway 後端運行狀態

### 問題：數據庫連接失敗

**解決方案：**
1. 檢查 `DATABASE_URL` 格式
2. 確認 Railway MySQL 服務運行正常
3. 檢查數據庫遷移是否執行

### 問題：構建失敗

**解決方案：**
1. 檢查 `package.json` 依賴
2. 確認構建命令正確
3. 查看構建日誌

---

## ✨ 部署檢查清單

### Railway 後端
- [ ] 項目已創建
- [ ] MySQL 數據庫已添加
- [ ] 環境變量已配置
- [ ] 數據庫遷移已執行
- [ ] 服務正常運行
- [ ] 後端 URL 已獲取

### Vercel 前端
- [ ] 項目已導入
- [ ] 構建設置正確
- [ ] 環境變量已配置
- [ ] API 代理已配置
- [ ] 部署成功
- [ ] 前端 URL 可訪問

### 測試驗證
- [ ] 前端頁面正常
- [ ] API 請求成功
- [ ] 數據庫連接正常
- [ ] 沒有錯誤

---

**完整部署文檔請參考：** [DEPLOYMENT.md](./DEPLOYMENT.md)

**環境變量說明請參考：** [ENV_VARIABLES.md](./ENV_VARIABLES.md)
